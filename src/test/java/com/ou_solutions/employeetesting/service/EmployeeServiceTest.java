package com.ou_solutions.employeetesting.service;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.aspectj.lang.annotation.Before;
import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import com.ou_solutions.employeetesting.entity.Employee;
import com.ou_solutions.employeetesting.repository.EmployeeRepository;


@SpringBootTest
public class EmployeeServiceTest {
	/*
	 * 
	 * @InjectMocks EmployeeService employeeService;
	 * 
	 * @Autowired private EmployeeRepository empRepository;
	 * 
	 * 
	 * 
	 * @Test public void getEmployeeTest() throws NoSuchFieldException,
	 * SecurityException, IllegalArgumentException, IllegalAccessException {
	 * 
	 * 
	 * 
	 * Employee emp = employeeService.getEmployee(1L); if(emp != null)
	 * assertEquals(emp.getFullName(), "Omkar Upadhyay");
	 * 
	 * if(emp == null) assertEquals(emp, null);
	 * 
	 * }
	 * 
	 * 
	 * 
	 */}
